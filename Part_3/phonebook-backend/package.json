{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "backend web api for phonebook app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../Part_2/phonebook && npm run build && cp -r dist ../../Part_3/phonebook-backend && rm -rf dist",
    "build:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.18.2",
    "morgan": "^1.10.1"
  }
}
